# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The screenshot baseline. `scripts/baseline.mjs` regenerates it; the script is
# what is committed, because a folder of pictures nobody can remake is a folder
# of pictures nobody dares delete.
shots

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# What scripts/mirror.mjs fetches — a copy of the deployed site, not source.
mirror

# The gateway's action journal (server/institution/start.ts writes here by
# default). It holds encrypted prepared actions and a cleartext audit table of
# who did what at which school — a development copy is still somebody's data.
work/
